# Message Queueing

System for queueing messages.

## Build and test application

Test the codebase: 

```shell
make test
```

Build the application: 

```shell
make buildApp
```

Build a Docker image:

```shell
make buildImage
```
